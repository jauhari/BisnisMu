"use client";
import { WorkspaceHeader, SplitWorkspace, DetailPanel } from "@/components/layout/workspace";
import { GlassErrorState, GlassSkeleton } from "@/components/feedback/glass-feedback";
import { GlassTable } from "@/components/tables/glass-table";
import { useListQuery } from "@/presentation/query/dashboard-hooks";
export default function Page() { const { data, isLoading, error } = useListQuery<any[]>("/api/pos/sessions", ["list", "pos-sessions"]); if (isLoading) return <GlassSkeleton className="h-72" />; if (error || !data) return <GlassErrorState title="POS sessions unavailable" description="Unable to load POS sessions." />; return <div className="grid gap-6"><WorkspaceHeader eyebrow="POS" title="POS Sessions" description="Open, monitor, and close cashier sessions with linked cash drawer lifecycle." action={<button className="h-11 rounded-md bg-foreground px-4 text-sm font-medium text-background">Open session</button>} /><SplitWorkspace main={<GlassTable columns={[{ key: "session", header: "Session" }, { key: "terminal", header: "Terminal" }, { key: "opened", header: "Opened" }, { key: "expected", header: "Expected" }, { key: "status", header: "Status" }]} rows={data.data} empty="No POS sessions loaded" />} side={<DetailPanel title="Cash drawer link">Opening or closing a POS session delegates to CashService when a cash drawer is configured.</DetailPanel>} /></div>; }
