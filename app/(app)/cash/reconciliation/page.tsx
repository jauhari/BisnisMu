"use client";
import { WorkspaceHeader, SplitWorkspace, DetailPanel } from "@/components/layout/workspace";
import { GlassErrorState, GlassSkeleton } from "@/components/feedback/glass-feedback";
import { GlassTable } from "@/components/tables/glass-table";
import { useListQuery } from "@/presentation/query/dashboard-hooks";
export default function Page() { const { data, isLoading, error } = useListQuery<any[]>("/api/cash/reconciliation", ["list", "cash-reconciliation"]); if (isLoading) return <GlassSkeleton className="h-72" />; if (error || !data) return <GlassErrorState title="Cash reconciliation unavailable" description="Unable to load reconciliation records." />; return <div className="grid gap-6"><WorkspaceHeader eyebrow="Cash Management" title="Cash Reconciliation" description="Cash reconciliation workflow routed through CashService/CashManagementService with journal preview and audit trace." action={<button className="h-11 rounded-md bg-foreground px-4 text-sm font-medium text-background">Create reconciliation</button>} /><SplitWorkspace main={<GlassTable columns={[{ key: "number", header: "Number" }, { key: "account", header: "Account" }, { key: "date", header: "Date" }, { key: "amount", header: "Amount" }, { key: "status", header: "Status" }]} rows={data.data} empty="No cash reconciliation loaded" />} side={<DetailPanel title="Controls">Reconciliation differences remain journal-traceable.</DetailPanel>} /></div>; }
