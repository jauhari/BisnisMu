"use client";
import { WorkspaceHeader, SplitWorkspace, DetailPanel } from "@/components/layout/workspace";
import { GlassErrorState, GlassSkeleton } from "@/components/feedback/glass-feedback";
import { GlassTable } from "@/components/tables/glass-table";
import { useListQuery } from "@/presentation/query/dashboard-hooks";
export default function Page() { const { data, isLoading, error } = useListQuery<any[]>("/api/ar-ap/receivables", ["list", "ar-ap-receivables"]); if (isLoading) return <GlassSkeleton className="h-72" />; if (error || !data) return <GlassErrorState title="Receivables unavailable" description="Unable to load receivables aging." />; return <div className="grid gap-6"><WorkspaceHeader eyebrow="AR/AP" title="Receivables Aging" description="Outstanding customer invoices grouped by aging buckets from ArApService." action={<button className="h-11 rounded-md bg-foreground px-4 text-sm font-medium text-background">Export aging</button>} /><SplitWorkspace main={<GlassTable columns={[{ key: "customer", header: "Customer" }, { key: "current", header: "Current" }, { key: "d30", header: "1-30" }, { key: "d60", header: "31-60" }, { key: "d90", header: "61-90" }, { key: "over", header: ">90" }]} rows={data.data} empty="No receivables loaded" />} side={<DetailPanel title="Drilldown">Open invoice, payment history, and source journal trace from each aging row.</DetailPanel>} /></div>; }
