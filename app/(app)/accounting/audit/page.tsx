"use client";
import { WorkspaceHeader, SplitWorkspace, DetailPanel } from "@/components/layout/workspace";
import { GlassErrorState, GlassSkeleton } from "@/components/feedback/glass-feedback";
import { GlassTable } from "@/components/tables/glass-table";
import { useListQuery } from "@/presentation/query/dashboard-hooks";
export default function Page() { const { data, isLoading, error } = useListQuery<any[]>("/api/accounting/audit", ["list", "accounting-audit"]); if (isLoading) return <GlassSkeleton className="h-72" />; if (error || !data) return <GlassErrorState title="Audit unavailable" description="Unable to load audit trail." />; return <div className="grid gap-6"><WorkspaceHeader eyebrow="Accounting" title="Audit Trail" description="Tenant-scoped audit records for accounting, cash, revenue, inventory, and reporting actions." action={<button className="h-11 rounded-md bg-foreground px-4 text-sm font-medium text-background">Export audit</button>} /><SplitWorkspace main={<GlassTable columns={[{ key: "time", header: "Time" }, { key: "actor", header: "Actor" }, { key: "action", header: "Action" }, { key: "entity", header: "Entity" }, { key: "metadata", header: "Metadata" }]} rows={data.data} empty="No audit events loaded" />} side={<DetailPanel title="Audit scope">Current schema stores tenant-level audit logs. Platform-level audit requires future auth/admin schema.</DetailPanel>} /></div>; }
