"use client";
import { WorkspaceHeader, SplitWorkspace, DetailPanel } from "@/components/layout/workspace";
import { GlassErrorState, GlassSkeleton } from "@/components/feedback/glass-feedback";
import { GlassTable } from "@/components/tables/glass-table";
import { useListQuery } from "@/presentation/query/dashboard-hooks";
export default function Page() { const { data, isLoading, error } = useListQuery<any[]>("/api/inventory/movements", ["list", "inventory-movements"]); if (isLoading) return <GlassSkeleton className="h-72" />; if (error || !data) return <GlassErrorState title="Inventory movements unavailable" description="Unable to load inventory movements." />; return <div className="grid gap-6"><WorkspaceHeader eyebrow="Inventory" title="Inventory Movements" description="Inventory movements workflow connected to InventoryService with journal and cost traceability." /><SplitWorkspace main={<GlassTable columns={[{ key: "product", header: "Product" }, { key: "date", header: "Date" }, { key: "quantity", header: "Quantity" }, { key: "cost", header: "Cost" }, { key: "journal", header: "Journal" }]} rows={data.data} empty="No inventory movements loaded" />} side={<DetailPanel title="Traceability">Movement rows open product, cost history, and journal source drawers.</DetailPanel>} /></div>; }
