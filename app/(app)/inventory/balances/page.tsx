"use client";
import { WorkspaceHeader } from "@/components/layout/workspace";
import { GlassErrorState, GlassSkeleton } from "@/components/feedback/glass-feedback";
import { GlassStatsCard } from "@/components/glass/glass-primitives";
import { GlassTable } from "@/components/tables/glass-table";
import { useListQuery } from "@/presentation/query/dashboard-hooks";
export default function Page() { const { data, isLoading, error } = useListQuery<any[]>("/api/inventory/balances", ["list", "inventory-balances"]); if (isLoading) return <GlassSkeleton className="h-72" />; if (error || !data) return <GlassErrorState title="Inventory balances unavailable" description="Unable to load stock balances." />; const rows = data.data; const inventoryValue = rows.reduce((sum: number, row: any) => sum + Number(row.value ?? 0), 0); const totalQty = rows.reduce((sum: number, row: any) => sum + Number(row.quantity ?? 0), 0); const locations = new Set(rows.map((row: any) => row.location)).size; return <div className="grid gap-6"><WorkspaceHeader eyebrow="Inventory" title="Stock Balances" description="Stock quantity, average cost, and inventory value by product and location." action={<button className="h-11 rounded-md bg-foreground px-4 text-sm font-medium text-background">Adjust stock</button>} /><section className="grid gap-4 md:grid-cols-3"><GlassStatsCard title="Inventory value" value={String(inventoryValue)} detail="Average cost basis" /><GlassStatsCard title="Total quantity" value={String(totalQty)} detail="Units on hand" /><GlassStatsCard title="Locations" value={String(locations)} detail="Warehouses" /></section><GlassTable columns={[{ key: "product", header: "Product" }, { key: "location", header: "Location" }, { key: "quantity", header: "Quantity" }, { key: "averageCost", header: "Average cost" }, { key: "value", header: "Value" }]} rows={rows} empty="No balances loaded" /></div>; }
